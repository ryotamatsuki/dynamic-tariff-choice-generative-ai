# Stage 10 — Section-by-Section Paper Construction

Date: 2026-09-12 (JST)
Workflow: `ryotamatsuki/research-paper-workflow` v2.1
Project: **Dynamic Tariff Choice for Generative AI: Model Improvement, Commitment, and Welfare**

## Verdict

**FULL DRAFT READY FOR REFEREE GATE.**

Stage 10 is complete. Stage 11 — Robustness / Referee Attack Gate is authorized. No theory change was made in Stage 10.

## 1. Starting state and authoritative inputs

Stage-10 work started from repository head `62aa29a24c425a84a8f1c336da357f27f949ed80`, after Stage 9 had closed with `REPRODUCIBILITY BASELINE READY`. The Stage-8 theory freeze at `2597e82044ec94a58fad033227ea415e64af8c6d` remained authoritative throughout and remains an ancestor of the manuscript head.

The controlling scientific records are:

- `docs/STAGE_08_THEORY_FREEZE.md`;
- `docs/freeze/MODEL_REGISTER.md`;
- `docs/freeze/PROPOSITION_SCOPE_REGISTER.md`;
- `docs/freeze/WELFARE_BENCHMARK_REGISTER.md`;
- `docs/freeze/VERIFICATION_REGISTER.md`;
- `docs/freeze/CONTRIBUTION_REGISTER.md`;
- `theorem_certificates/current_scope.md`;
- `formal/FORMAL_VERIFICATION_CERTIFICATE.md`.

## 2. Full manuscript map

The manuscript is now modular and complete at the Stage-10 level:

- `sections/00_abstract.tex` — abstract;
- `sections/01_introduction.tex` — motivation, mechanism, contribution, scope and roadmap;
- `sections/02_model.tex` — primitives, tariff family, timing, equilibrium concept and regular region `R`;
- `sections/03_equilibrium.tex` — continuation pricing, architecture-sensitive integration, T1–T3, mixed resolution and threshold-collapse benchmark;
- `sections/04_welfare.tex` — first-best benchmark, decentralized welfare, W1 fixed-installed-base wedge and endogenous sign-indeterminacy;
- `sections/05_robustness.tex` — general-CDF sufficient conditions, nonquadratic numerical robustness, outside-`R` scope guard and formal-verification boundary;
- `sections/06_institutional.tex` — enterprise-AI interpretation with explicit evidence limits;
- `sections/07_related_literature.tex` — closest-literature distinction;
- `sections/08_discussion.tex` — interpretation, comparative implications, conditional quality path, policy limits and omissions;
- `sections/09_conclusion.tex` — conclusion;
- `sections/10_appendix.tex` — derivations and proofs of continuation pricing, T1, T2, T3, mixed-probability bounds, threshold collapse, W1, and the exact `23/10` outside-region counterexample.

The manuscript entry point is `paper/main.tex`.

## 3. Frozen claims carried into the paper

The full draft states only the certified claims:

- T1: on the certified regular both-served region `R`, `0<h_M<h_F`;
- T2: on `R`, `mu_M<mu_F`;
- T3: for strict `mu_M<mu<mu_F`, neither pure expectation is self-consistent and the certified regular branch has a unique mixed resolution;
- B1: if integration is architecture-insensitive, the two thresholds collapse;
- W1: holding the installed base fixed, `mu_P-mu_W=d h p*(h)>0`;
- full endogenous architecture-commitment welfare has no unconditional sign in the frozen baseline.

The draft does not claim arbitrary-concave-demand generality, global equilibrium characterization outside `R`, universal quality-driven tariff transitions, or unconditional welfare superiority.

## 4. Figure / table architecture gate

Canonical exposition map: `docs/STAGE_10_EXPOSITION_MAP.md`.

Exactly one substantive figure and one generated numerical table are required at this stage:

- `figures/threshold_phase.tex` — the two self-consistency thresholds and intermediate pure-regime gap;
- `tables/baseline_example.tex` — exact and decimal objects for the permanent certified regular example.

Both are deterministically generated by `scripts/generate_outputs.py`. Welfare and robustness results remain theorem/prose objects because additional calibration graphics would risk communicating a stronger claim than the frozen scope supports.

## 5. Literature and source verification

Canonical record: `docs/STAGE_10_SOURCE_VERIFICATION.md`.

The manuscript bibliography uses source-checked metadata for the static nonlinear-pricing benchmark, dynamic subscription/adoption, application/platform hold-up, proportional-fee commitment, and the recent LLM/cloud pricing frontier. The strongest adjacent contributions are treated as novelty threats rather than decorative citations.

The retained contribution statement is narrow: anticipated tariff architecture changes sunk integration, and that installed state changes the provider's later architecture incentive, splitting one static switching threshold into two self-consistency thresholds. The paper does not claim novelty for generic hold-up, generic commitment, two-part tariffs, costly inference, or the mere existence of mixed equilibrium.

## 6. Verification and build evidence

The substantive full-draft commit is `dec3b31af1484b8057a586fd9486bf3a7dafc652`.

For that draft:

- reproducibility workflow run `34665397607`: PASS;
- exact symbolic and robustness checks: PASS;
- permanent regression tests: PASS;
- freeze/scope integrity gate: PASS;
- deterministic figure/table generation: PASS;
- LaTeX/BibTeX build: PASS;
- generated manuscript PDF: 15 pages;
- verify workflow run `34665397694`: PASS;
- explicit Lean target and no-`sorry`/`admit` audit: PASS.

After bibliography completion, Stage-10 integrity expansion, obsolete-placeholder removal, and appendix-heading cleanup, the current-manuscript verification remained green on the post-draft heads, including `verify` run `34665604839` and `reproducibility` run `34665604864`.

## 7. Exposition and claim-scope checks

`scripts/check_integrity.py` now fails the repository gate if the full Stage-10 section set or Stage-10 records are absent, if the Stage-8 freeze is no longer an ancestor, if the permanent `23/10` counterexample or arbitrary-concave-demand scope warning disappears, or if the manuscript loses T1/T2/T3/W1 and the required figure/table labels.

The formal artifact is described only as proof-critical-core verification. The appendix gives human-readable algebra and does not use the Lean artifact as a substitute for economic derivation.

## 8. Remaining manuscript gaps

There is no Stage-10 scientific blocker. Journal-specific length, style, title refinement, author metadata and submission formatting are downstream tasks. Target-journal positioning is intentionally deferred until after the Stage-11 hostile referee gate.

## 9. Exact Stage-11 contract

Stage 11 must treat the completed manuscript as an object to attack, not as an accepted result. It must independently test novelty/mechanism, assumptions and mathematical validity, continuation completeness, candidate deviations versus alternative equilibria, indifference/multiplicity, welfare-selection robustness, theorem quantifiers/function classes, institutional interpretation, journal fit and exposition/claim inflation.

At least one material equilibrium candidate must be re-audited from primitives, and an alternative-equilibrium search must be performed separately. Sequential continuations and active-set changes must be attacked independently rather than by reusing the production solver. At least one high-stakes generality claim must be attacked outside the baseline functional form where applicable. Any flaw that invalidates Stage 4A, Stage 7, or Stage 7.5A certification is a certification regression and must roll the paper back to the earliest affected stage under `docs/CHANGE_CONTROL.md`.

Stage 11 may recommend exposition fixes, additional verification, or a bounded approved robustness exercise. It must not add unmotivated extensions. Its canonical verdict must be one of `GO TO JOURNAL POSITIONING`, `CONDITIONAL GO`, or `REOPEN EARLIER STAGE / NO-GO`.

## Final routing

**FULL DRAFT READY FOR REFEREE GATE — GO TO STAGE 11.**

The commit adding this record and synchronizing project status is the Stage-10 administrative closeout point.
# Stage 10 Figure/Table Architecture Gate

Date: 2026-09-12 JST.

Authority: repaired Stage-8 freeze and repaired Stage-9 reproducibility baseline.

| Result | Primary vehicle | Source | Final paper |
|---|---|---|---|
| T1: h0 < hM < hF on strict R+ | theorem plus exact-example table | proposition register; Stage-4A audit; output generator | required |
| T2: muM < muF | theorem plus threshold figure | baseline verification; output generator | required |
| T3: no pure architecture in the strict gap | proposition plus threshold figure and exact-example table | Stage-4A certificates; output generator | required |
| B1: architecture-insensitive integration collapses the split | corollary plus prose | regression test; current scope certificate | required |
| W1: private/social fixed-base wedge | proposition plus algebra | welfare register; formal core | required |
| W2: endogenous welfare ranking is sign-indeterminate | concise prose | repaired Stage-7 verification | required |
| strict R+ scope and candidate interval [h0,hF] | model restrictions plus robustness discussion | repaired model register | required |
| outside-domain 23/10 H-only counterexample | exact numerical illustration | regression test and independent audit | required |
| common-curvature nonquadratic survival | concise prose only | robustness checks | optional |
| arbitrary strict concavity | limitation/scope statement | repaired Stage-7.5A record | required |

The final Stage-10 manuscript uses one substantive figure, the two-threshold regime map, and one quantitative table, the frozen exact example. Both are generated deterministically by scripts/generate_outputs.py. No decorative visual is required. The figure caption must make clear that the numerical interval is the frozen exact example and not a theorem outside strict R+.